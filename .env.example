# PostgreSQL connection string from Neon, Supabase, or another provider
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/ledgerly?sslmode=require"

# Create a long random value before deploying: openssl rand -base64 32
JWT_SECRET="replace-with-a-long-random-secret"
